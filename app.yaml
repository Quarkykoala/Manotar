runtime: python39
entrypoint: gunicorn -b :$PORT app:app

env_variables:
  FLASK_APP: app
  FLASK_ENV: production

beta_settings:
  cloud_sql_instances: mental-health-bot-430515:asia-south1:manobal-01

handlers:
  - url: /.*
    script: auto
